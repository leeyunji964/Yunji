이걸 했을 때 => 결과물이 어느정도 성과물을 얻을 수 있을지 예측까지!

모델을 만드는 건 필수가 아니지만, 만들어 보는 게,, 


Modeling : Model을 만들어 내는 과정.
-> 현실세계를 표현하는 수식( 통계적 관점의 data model )을 도출하는 과정.
* 가장 기본적인 수식 형태 :  y = wx + b
	x : 독립변수(=특성=설명변수)
	y : 반응변수(lable)

x, y = 매개변수 = 계수
w, b <- 모델링 과정을 통해 찾아내야 하는 변수.


< modeling 과정 > : 회귀식 : Regression expression
y = wx + b	w =  기울기(weight), b = 편향(bias) => 상수형태(w,b) 
1. model 선택. 수식 결정
2. 실제 data(y,x : 변수)를 이용하여 w,b(상수) 값을 결정
=> "훈련한다." or "훈련과정"
* w,b를 구할 수 없으면 => 적합한 모델이 아니다!

3. 실제  data를 통한 예측
★4. 모델 평가 : 데이터 분석을 잘 해석할 수 있는 모델이냐,, 아니냐,, 평가,, 


[회귀식 : Regression expression]
1. 단순 선형회귀분석 : y = wx + b <- 예측에 사용
2. 다중 선형회귀분석

p.341 - p.365 : 통계 이론
p.371 - : 기술통계 분석
p.489 ~ : 모델링


[오후]
회귀 Model

* 단순 선형 회귀 분석 : 하나의 독립 변수 예측 model






























