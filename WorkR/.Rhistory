wordcloud2( pwordcount, size = 3 )
steve <- readLines("ex_10-4.txt", encoding = 'UTF-8')
steve
steve.noun <- sapply( steve, extractNoun, USE.NAMES = F )
head(steve.noun)
sn <- unlist( steve.noun ); sn
swordcount <- table(sn)
sort.sn <- sort( swordcount, decreasing = T )
head(sort.sn)
sort.sn
sort.sn <- sort.sn[-3]
head(sort.sn)
wordcloud( names(swordcount), freq = swordcount,
scale = c( 0.1, 2), min.freq = 4,
random.order = F, rot.per = 0.1, colors = pal3)
wordcloud( names(swordcount), freq = swordcount,
scale = c( 0.1, 2), min.freq = 3,
random.order = F, rot.per = 0.1, colors = pal3)
wordcloud( names(swordcount), freq = swordcount,
scale = c( 0.1, 2), min.freq = 2,
random.order = F, rot.per = 0.1, colors = pal3)
wordcloud( names(swordcount), freq = swordcount,
scale = c( 0.1, 2), min.freq = 3,
random.order = F, rot.per = 0.1, colors = pal3)
wordcloud( names(swordcount), freq = swordcount,
scale = c( 0.1, 1), min.freq = 3,
random.order = F, rot.per = 0.1, colors = pal3)
wordcloud( names(swordcount), freq = swordcount,
scale = c( 0.1, 1.8), min.freq = 3,
random.order = F, rot.per = 0.1, colors = pal3)
wordcloud( names(swordcount), freq = swordcount,
scale = c( 0.1, 1.8), min.freq = 3,
random.order = F, rot.per = 0.1, colors = pal2)
wordcloud( names(swordcount), freq = swordcount,
scale = c( 0.1, 2), min.freq = 3,
random.order = F, rot.per = 0.1, colors = pal2)
wordcloud2( swordcount, size = 3 )
obama <- readLines("ex_10-5.txt", encoding = 'UTF-8' )
head(obama)
obama.noun <- ssapply( obama, extractNoun, USE.NAMES = F )
obama.noun <- sapply( obama, extractNoun, USE.NAMES = F )
head(obama.noun)
on <- unlist(obama.noun); on
owordcount <- table(on);    head(owordcount)
owordcount <- table(on);    owordcount
sort.obama <- sort( owordcount, decreasing = T )
sort.obama
sort.obama <- sort.obama[-1]
sort.obama
wordcloud( names(owordcount), freq = owordcount,
min.freq = 3, scale = c( 0.1, 2),
random.order = F, rot.per = 0.1, colors = pal3)
wordcloud( names(owordcount), freq = owordcount,
min.freq = 3, scale = c( 0.1, 2),
random.order = F, rot.per = 0.1, colors = pal2)
wordcloud( names(owordcount), freq = owordcount,
min.freq = 3, scale = c( 0.1, 3),
random.order = F, rot.per = 0.1, colors = pal2)
wordcloud( names(owordcount), freq = owordcount,
min.freq = 3, scale = c( 0.3, 2),
random.order = F, rot.per = 0.1, colors = pal2)
wordcloud2( owordcount, size = 3 )
wordcloud2( owordcount, size = 2 )
head(sort.obama)
owordcount
noun
wordcount
# 빈도가 높은 순으로 정렬, 데이터가 많으니까 데이터 셋에서 10개만 추출
sort.noun
wordcloud( names( wordcount ),   # 단어
freq = wordcount,     # 단어 빈도
scale = c(6, 0.7),    # 단어 폰트 크기(최대, 최소)
min.freq = 3,         # 단어 최소 빈도 : 빈도가 3 이상인 것들만 출력하라.
random.order = F,     # 단어 출력 위치 : T로 나오면 무작위, F면 빈도가 높은 게 가운데 지정.
rot.per = .1,         # 90도 회전 단어비율 : .1 = 0.1 = 10%
colors = pal3)        # 단어 색
wordcloud( names(lwordcount), freq = lwordcount,
scale = c(0.1,2), min.freq = 3,
random.order = F, rot.per = 0.1, colors = pal3)
wordcloud( names(lwordcount), freq = lwordcount,
scale = c(0.1,2), min.freq = 5,
random.order = F, rot.per = 0.1, colors = pal3)
wordcloud( names(lwordcount), freq = lwordcount,
scale = c(0.1,2), min.freq = 4,
random.order = F, rot.per = 0.1, colors = pal3)
wordcount
lwordcount
wordcloud( names( wordcount ),   # 단어
freq = wordcount,     # 단어 빈도
scale = c(6, 0.7),    # 단어 폰트 크기(최대, 최소)
min.freq = 3,         # 단어 최소 빈도 : 빈도가 3 이상인 것들만 출력하라.
random.order = F,     # 단어 출력 위치 : T로 나오면 무작위, F면 빈도가 높은 게 가운데 지정.
rot.per = .1,         # 90도 회전 단어비율 : .1 = 0.1 = 10%
colors = pal3)        # 단어 색
wordcloud( names(lwordcount), freq = lwordcount,
scale = c(5,1), # 최대, 최소
min.freq = 4,
random.order = F, rot.per = 0.1, colors = pal3)
wordcloud( names(lwordcount), freq = lwordcount,
scale = c(8,0.5), # 최대, 최소
min.freq = 4,
random.order = F, rot.per = 0.1, colors = pal3)
wordcloud( names(lwordcount), freq = lwordcount,
scale = c(8,0.7), # 최대, 최소
min.freq = 4,
random.order = F, rot.per = 0.1, colors = pal3)
wordcloud2( lwordcount, size = 2 )
wordcloud( names(lwordcount), freq = lwordcount,
scale = c(10,1), # 최대, 최소
min.freq = 4,
random.order = F, rot.per = .1, colors = pal3)
wordcloud( names(cwordcount),
freq = cwordcount,
scale = c( 7, 0.5 ),
min.freq = 3,
random.order = F,
rot.per = 0.1,
colors = pal2)
wordcloud( names(cwordcount),
freq = cwordcount,
scale = c( 7, 1 ),
min.freq = 3,
random.order = F,
rot.per = 0.1,
colors = pal2)
wordcloud( names(cwordcount),
freq = cwordcount,
scale = c( 10, 1 ),
min.freq = 3,
random.order = F,
rot.per = 0.1,
colors = pal2)
pal2 <- brewer.pal( 9, 'Dark2' )
wordcloud( names(cwordcount),
freq = cwordcount,
scale = c( 10, 1 ),
min.freq = 3,
random.order = F,
rot.per = 0.1,
colors = pal2)
pal2 <- brewer.pal( 6, 'Dark2' )
wordcloud( names(cwordcount),
freq = cwordcount,
scale = c( 10, 1 ),
min.freq = 3,
random.order = F,
rot.per = 0.1,
colors = pal2)
wordcloud2( cwordcount, size = 3)
wordcloud2( cwordcount, size = 3, shape = 'triangle')
wordcloud( names(lwordcount), freq = lwordcount,
scale = c(10,1), # 최대, 최소
min.freq = 4,
random.order = F, rot.per = .1, colors = pal3)
wordcloud( names(cwordcount),
freq = cwordcount,
scale = c( 10, 1 ),
min.freq = 3,
random.order = F,
rot.per = 0.1,
colors = pal2)
wordcloud( names(pwordcount), freq = pwordcount,
scale = c( 10, 1), min.freq = 5,
random.order = F, rot.per = 0.1, colors = pal2)
wordcloud( names(pwordcount), freq = pwordcount,
scale = c( 15, 1), min.freq = 5,
random.order = F, rot.per = 0.1, colors = pal2)
wordcloud2( pwordcount, size = 3 )
wordcloud2( pwordcount, size = 3, shape = 'star' )
wordcloud( names(swordcount), freq = swordcount,
scale = c( 13, 1), min.freq = 3,
random.order = F, rot.per = 0.1, colors = pal2)
wordcloud( names(swordcount), freq = swordcount,
scale = c( 10, 1), min.freq = 3,
random.order = F, rot.per = 0.1, colors = pal2)
wordcloud( names(swordcount), freq = swordcount,
scale = c( 10, 0.5), min.freq = 3,
random.order = F, rot.per = 0.1, colors = pal2)
wordcloud( names(swordcount), freq = swordcount,
scale = c( 8, 0.5), min.freq = 3,
random.order = F, rot.per = 0.1, colors = pal2)
wordcloud( names(swordcount), freq = swordcount,
scale = c( 8, 1), min.freq = 3,
random.order = F, rot.per = 0.1, colors = pal2)
wordcloud2( swordcount, size = 3 )
wordcloud( names(owordcount), freq = owordcount,
min.freq = 3, scale = c( 8, 0.5 ),
random.order = F, rot.per = 0.1, colors = pal2)
wordcloud( names(owordcount), freq = owordcount,
min.freq = 3, scale = c( 10, 1 ),
random.order = F, rot.per = 0.1, colors = pal2)
wordcloud( names(owordcount), freq = owordcount,
min.freq = 3, scale = c( 10, 1 ),
random.order = F, rot.per = 0.2, colors = pal2)
wordcloud( names(owordcount), freq = owordcount,
min.freq = 3, scale = c( 9, 1 ),
random.order = F, rot.per = 0.2, colors = pal2)
wordcloud( names(owordcount), freq = owordcount,
min.freq = 3, scale = c( 9, 0.7 ),
random.order = F, rot.per = 0.2, colors = pal2)
wordcloud2( owordcount, size = 2 )
wordcloud2( owordcount, color = 'random-light',
backgroundColor = 'skyblue',
size = 2 )
wordcloud2( owordcount, color = 'random-light',
backgroundColor = 'black',
size = 2 )
wordcloud( names(cwordcount),
freq = cwordcount,
scale = c( 10, 1 ),
min.freq = 3,
random.order = F,
rot.per = 0.1,
colors = pal2)
cwordcount
# 불필요한 단어 삭제
chu.n <- chu.n[ nchar(noun2chu.n) > 1 ]
# 불필요한 단어 삭제
chu.n <- chu.n[ nchar(chu.n) > 1 ]
chu.n <- gsub('◇','',chu.n)
chu.n <- gsub('년','',chu.n)
chu.n <- gsub('되','',chu.n)
chu.n <- gsub('드','',chu.n)
chu.n <- gsub('명','',chu.n)
chu.n <- gsub('만','',chu.n)
chu.n <- gsub('아','',chu.n)
chu.n <- gsub('오','',chu.n)
chu.n <- gsub('조','',chu.n)
chu.n <- gsub('중','',chu.n)
chu.n <- gsub('지','',chu.n)
chu.n <- gsub('패착입니다','패착',chu.n)
cwordcount <- table(chu.n)
cwordcount
wordcloud( names(cwordcount),
freq = cwordcount,
scale = c( 10, 1 ),
min.freq = 3,
random.order = F,
rot.per = 0.1,
colors = pal2)
wordcloud( names(cwordcount),
freq = cwordcount,
scale = c( 8, 1 ),
min.freq = 3,
random.order = F,
rot.per = 0.1,
colors = pal2)
wordcloud( names(cwordcount),
freq = cwordcount,
scale = c( 7, 0.5 ),
min.freq = 3,
random.order = F,
rot.per = 0.1,
colors = pal2)
wordcloud( names(cwordcount),
freq = cwordcount,
scale = c( 8, 0.5 ),
min.freq = 3,
random.order = F,
rot.per = 0.1,
colors = pal2)
wordcloud2( cwordcount, size = 3, shape = 'triangle')
wordcloud2( cwordcount, size = 3, backgroundColor = "black")
wordcloud2( cwordcount, size = 2 )
chu.n <- gsub('때문','',chu.n)
cwordcount <- table(chu.n)
cwordcount
pal2 <- brewer.pal( 6, 'Dark2' )
wordcloud( names(cwordcount),
freq = cwordcount,
scale = c( 8, 0.5 ),
min.freq = 3,
random.order = F,
rot.per = 0.1,
colors = pal2)
wordcloud2( cwordcount, size = 2 )
wordcloud( names(lwordcount), freq = lwordcount,
scale = c(10,1), # 최대, 최소
min.freq = 4,
random.order = F, rot.per = .1, colors = pal3)
wordcloud2( lwordcount, size = 2 )
lee.n <- lee.n[ nchar(lee.n) > 1 ]
lee.n <- gsub('것','',lee.n)
lee.n <- gsub('되','',lee.n)
lee.n <- gsub('저','',lee.n)
lee.n <- gsub('수','',lee.n)
lee.n <- gsub('하','',lee.n)
lee.n <- gsub('등','',lee.n)
lee.n <- gsub('년','',lee.n)
lee.n <- gsub('번','',lee.n)
lee.n <- gsub('중','',lee.n)
lwordcount <- table(lee.n)
lwordcount
lee.n <- lee.n[ nchar(lee.n) > 1 ]
lee.n <- gsub('것','',lee.n)
lee.n <- gsub('되','',lee.n)
lee.n <- gsub('저','',lee.n)
lee.n <- gsub('수','',lee.n)
lee.n <- gsub('하','',lee.n)
lee.n <- gsub('등','',lee.n)
lee.n <- gsub('년','',lee.n)
lee.n <- gsub('번','',lee.n)
lee.n <- gsub('중','',lee.n)
lee.n <- gsub('거','',lee.n)
lee.n <- gsub('고','',lee.n)
lee.n <- gsub('그','',lee.n)
lee.n <- gsub('금','',lee.n)
lee.n <- gsub('긍','',lee.n)
lee.n <- gsub('라','',lee.n)
lee.n <- gsub('내','',lee.n)
lee.n <- gsub('보','',lee.n)
lee.n <- gsub('시','',lee.n)
lee.n <- gsub('중','',lee.n)
lee.n <- gsub('해왔던국회개혁과','국회개혁',lee.n)
lwordcount <- table(lee.n)
lwordcount
pal3 <- brewer.pal( 9, 'Blues')[5:9]
wordcloud( names(lwordcount), freq = lwordcount,
scale = c(10,1), # 최대, 최소
min.freq = 4,
random.order = F, rot.per = .1, colors = pal3)
wordcloud( names(lwordcount), freq = lwordcount,
scale = c(8,1), # 최대, 최소
min.freq = 4,
random.order = F, rot.per = .1, colors = pal3)
wordcloud2( lwordcount, size = 2 )
sort.park
park.n <- park.n[ nchar(park.n) > 1 ]
park.n <- gsub('하','',park.n)
park.n <- gsub('것','',park.n)
park.n <- gsub('수','',park.n)
park.n <- gsub('한','',park.n)
park.n <- gsub('드','',park.n)
park.n <- gsub('저','',park.n)
park.n <- gsub('년','',park.n)
park.n <- gsub('대','',park.n)
park.n <- gsub('원','',park.n)
park.n <- gsub('하','',park.n)
park.n <- gsub('◇','',park.n)
park.n <- gsub('만','',park.n)
park.n <- gsub('때문','',park.n)
park.n <- gsub('등','',park.n)
park.n <- gsub('주도하','주도',park.n)
park.n <- gsub("'국민",'국민',park.n)
park.n <- gsub("'대통령",'대통령',park.n)
park.n <- gsub('40kg에','40kg',park.n)
park.n <- gsub('국민여러분께서는','국민여러분',park.n)
park.n <- gsub('더불어민주당에게도','더불어민주당',park.n)
pwordcount <- table(park.n)
pwordcount
park.n <- gsub('통령의','통령',park.n)
pwordcount <- table(park.n)
pwordcount
wordcloud( names(pwordcount), freq = pwordcount,
scale = c( 15, 1), min.freq = 5,
random.order = F, rot.per = 0.1, colors = pal2)
park.n <- park.n[ nchar(park.n) > 1 ]
park.n <- gsub('하','',park.n)
park.n <- gsub('것','',park.n)
park.n <- gsub('수','',park.n)
park.n <- gsub('한','',park.n)
park.n <- gsub('드','',park.n)
park.n <- gsub('저','',park.n)
park.n <- gsub('년','',park.n)
park.n <- gsub('대','',park.n)
park.n <- gsub('원','',park.n)
park.n <- gsub('하','',park.n)
park.n <- gsub('◇','',park.n)
park.n <- gsub('만','',park.n)
park.n <- gsub('때문','',park.n)
park.n <- gsub('등','',park.n)
park.n <- gsub('주도하','주도',park.n)
park.n <- gsub("'국민",'국민',park.n)
park.n <- gsub("'대통령",'대통령',park.n)
park.n <- gsub('40kg에','40kg',park.n)
park.n <- gsub('국민여러분께서는','국민여러분',park.n)
park.n <- gsub('더불어민주당에게도','더불어민주당',park.n)
park.n <- gsub('통령의','통령',park.n)
pwordcount <- table(park.n)
pwordcount
wordcloud( names(pwordcount), freq = pwordcount,
scale = c( 15, 1), min.freq = 5,
random.order = F, rot.per = 0.1, colors = pal2)
wordcloud( names(pwordcount), freq = pwordcount,
scale = c( 8, 1), min.freq = 5,
random.order = F, rot.per = 0.1, colors = pal2)
wordcloud2( pwordcount, size = 3, shape = 'star' )
sort.sn
sort.sn
sort.sn <- sort.sn[ nchar(sort.sn) > 1 ]
sort.sn <- gsub('것','', sort.sn)
sort.sn <- gsub('한','', sort.sn)
sort.sn <- gsub('때','', sort.sn)
sort.sn <- gsub('내','', sort.sn)
sort.sn <- gsub('저','', sort.sn)
sort.sn <- gsub('하','', sort.sn)
sort.sn <- gsub('거','', sort.sn)
sort.sn <- gsub('되','', sort.sn)
sort.sn <- gsub('지','', sort.sn)
sort.sn <- gsub('몇','', sort.sn)
sort.sn <- gsub('후','', sort.sn)
sort.sn <- gsub('할','', sort.sn)
swordcount <- table(sort.sn)
swordcount
wordcloud( names(swordcount), freq = swordcount,
scale = c( 8, 1), min.freq = 3,
random.order = F, rot.per = 0.1, colors = pal2)
steve <- readLines("ex_10-4.txt", encoding = 'UTF-8')
steve
steve.noun <- sapply( steve, extractNoun, USE.NAMES = F )
head(steve.noun)
sn <- unlist( steve.noun ); sn
swordcount <- table(sn)
sort.sn <- sort( swordcount, decreasing = T )
sort.sn <- sort.sn[-3]
sort.sn
sort.sn <- sort.sn[ nchar(sort.sn) > 1 ]
sort.sn <- gsub('것','', sort.sn)
sort.sn <- gsub('한','', sort.sn)
sort.sn <- gsub('때','', sort.sn)
sort.sn <- gsub('내','', sort.sn)
sort.sn <- gsub('저','', sort.sn)
sort.sn <- gsub('하','', sort.sn)
sort.sn <- gsub('거','', sort.sn)
sort.sn <- gsub('되','', sort.sn)
sort.sn <- gsub('지','', sort.sn)
sort.sn <- gsub('몇','', sort.sn)
sort.sn <- gsub('후','', sort.sn)
sort.sn <- gsub('할','', sort.sn)
swordcount <- table(sort.sn)
swordcount
steve.noun <- sapply( steve, extractNoun, USE.NAMES = F )
head(steve.noun)
steve <- readLines("ex_10-4.txt", encoding = 'UTF-8')
steve
steve.noun <- sapply( steve, extractNoun, USE.NAMES = F )
head(steve.noun)
steve <- readLines("ex_10-4.txt", encoding = 'UTF-8')
steve
steve.noun <- sapply( steve, extractNoun, USE.NAMES = F )
head(steve.noun)
sn <- unlist( steve.noun ); sn
swordcount <- table(sn)
sort.sn <- sort( swordcount, decreasing = T )
sort.sn <- sort.sn[-3]
sort.sn
swordcount <- table(sort.sn)
swordcount
wordcloud( names(swordcount), freq = swordcount,
scale = c( 8, 1), min.freq = 3,
random.order = F, rot.per = 0.1, colors = pal2)
steve <- readLines("ex_10-4.txt", encoding = 'UTF-8')
steve
steve <- readLines("ex_10-4.txt", encoding = 'UTF-8')
steve
steve.noun <- sapply( steve, extractNoun, USE.NAMES = F )
head(steve.noun)
sn <- unlist( steve.noun ); sn
swordcount <- table(sn)
sort.sn <- sort( swordcount, decreasing = T )
sort.sn
sort.sn <- sort.sn[-3]
sort.sn
wordcloud( names( sort.sn ), freq = sort.sn,
scale = c( 8, 1), min.freq = 3,
random.order = F, rot.per = 0.1, colors = pal2)
obama <- readLines("ex_10-5.txt", encoding = 'UTF-8' )
obama.noun <- sapply( obama, extractNoun, USE.NAMES = F )
head(obama.noun)
on <- unlist(obama.noun); on
owordcount <- table(on)
sort.obama <- sort( owordcount, decreasing = T )
sort.obama <- sort.obama[-1]
head(sort.obama)
sort.obama
sort.obama <- sort.obama[ nchar(sort.obama) > 1 ]
sort.obama <- gsub('것','',sort.obama)
sort.obama <- gsub('저','',sort.obama)
sort.obama <- gsub('들','',sort.obama)
sort.obama <- gsub('수','',sort.obama)
sort.obama <- gsub('한','',sort.obama)
sort.obama <- gsub('시','',sort.obama)
sort.obama <- gsub('년','',sort.obama)
sort.obama <- gsub('되','',sort.obama)
owordcount <- table(sort.obama)
owordcount
wordcloud( names(owordcount), freq = owordcount,
min.freq = 3, scale = c( 9, 0.7 ),
random.order = F, rot.per = 0.2, colors = pal2)
obama <- readLines("ex_10-5.txt", encoding = 'UTF-8' )
obama.noun <- sapply( obama, extractNoun, USE.NAMES = F )
head(obama.noun)
on <- unlist(obama.noun); on
owordcount <- table(on)
sort.obama <- sort( owordcount, decreasing = T )
sort.obama <- sort.obama[-1]
sort.obama
sort.obama <- sort.obama[ nchar(sort.obama) > 1 ]
sort.obama <- gsub('것','',sort.obama)
sort.obama <- gsub('저','',sort.obama)
sort.obama <- gsub('들','',sort.obama)
sort.obama <- gsub('수','',sort.obama)
sort.obama <- gsub('한','',sort.obama)
sort.obama <- gsub('시','',sort.obama)
sort.obama <- gsub('년','',sort.obama)
sort.obama <- gsub('되','',sort.obama)
wordcloud( names(sort.obama), freq = sort.obama,
min.freq = 3, scale = c( 9, 0.7 ),
random.order = F, rot.per = 0.2, colors = pal2)
wordcloud( names(sort.obama), freq = sort.obama,
min.freq = 3, scale = c( 9, 0.7 ),
random.order = F, rot.per = 0.2, colors = pal2)
